// 我们模块文件过多的时候， 应该需要有个存放这些模块的目录， Node 就很靠谱， 它规范我们可以将这些文件都放在 node_modules 目录中（ 大家都放在这个目录上， 就不会有其他乱七八糟的命名了）。
// 所以， 我们在 node_modules 中新建一个 03 _tool - multiply.js 文件， 其内容如下：

var tools = {
  multiply: (...numbers) => {
    let sum = numbers[0]

    for (let number in numbers) {
      sum = sum * numbers[number]
    }
    return sum
  }
}

module.exports = tools